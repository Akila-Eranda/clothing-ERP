# URL loaded by the Electron shell (local web or production tenant)
DESKTOP_APP_URL=http://localhost:3000

# Optional override for auto-update feed (folder that hosts latest.yml + HexaOne-Setup.exe)
# Default: {origin of DESKTOP_APP_URL}/downloads
# DESKTOP_UPDATE_URL=https://your-shop.hexalyte.com/downloads
